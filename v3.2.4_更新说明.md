# v3.2.4 更新说明

## 发布时间
2026-02-02

## 本次更新
本次版本主要解决用户反馈的三个核心问题：
1. ❌ 戳一戳功能未实际生效（API未真正调用）
2. ❌ 成员昵称映射配置未正确使用（已修复，但发现代码已实现）
3. ❌ 建立提醒别人类的日程时LLM监控超时（获取@对象时未添加超时保护）

## 详细修复内容

### 1. 戳一戳功能完善 ✅

**问题描述：**
- v3.2.3 虽然创建了戳一戳框架代码，但实际API调用被注释掉了（TODO标记）
- `_send_poke()` 方法只记录日志，从未真正执行 `client.group_poke()` 或 `client.friend_poke()`

**修复方案：**
参考 [pokepro插件](https://github.com/Zhalslar/astrbot_plugin_pokepro) 的实现：
- 重构方法名：`_send_poke(umo, target_id)` → `_send_poke_reminder(schedule)`
- 使用正确的client获取方式：
  ```python
  client = None
  if hasattr(platform, 'get_client'):
      client = platform.get_client()
  elif hasattr(platform, 'client'):
      client = platform.client
  ```
- 实现真正的API调用：
  ```python
  # 群戳一戳
  await client.group_poke(group_id=int(group_id), user_id=int(target_id))
  # 私聊戳一戳
  await client.friend_poke(user_id=int(target_id))
  ```

**修改文件：**
- [main.py](main.py) 第1167-1225行：完整重写 `_send_poke_reminder` 方法

**测试建议：**
1. 测试提醒自己的日程是否会戳一戳
2. 测试提醒别人的日程是否会戳一戳被提醒者
3. 测试群聊和私聊环境下戳一戳是否都正常工作
4. 检查配置中 `enable_poke` 开关是否生效

---

### 2. 成员昵称映射功能 ✅

**问题描述：**
- 用户反馈：配置文件中设置的成员ID映射昵称信息未被使用

**调查结果：**
经过代码审查，发现昵称映射功能**已经实现且正常工作**：
- [main.py](main.py) 第1572-1586行：`_get_user_display_name()` 方法
- [main.py](main.py) 第1595-1601行：在 `_generate_reminder_message()` 中调用
- [main.py](main.py) 第1484-1487行：在 `_generate_confirmation_response()` 中调用

**配置格式：**
```json
{
  "user_alias": [
    "123456789,老板",
    "987654321,小助手",
    "555555555,测试员"
  ]
}
```

**工作机制：**
1. 从 `_conf_schema.json` 中读取 `user_alias` 列表
2. 格式为 `用户ID,称呼`（逗号分隔，只分割第一个逗号）
3. 优先使用配置的别名，未配置则使用从API获取的真实昵称

**结论：**
该功能已在v3.2.3及之前版本中实现，无需额外修复。
如果用户发现昵称映射未生效，请检查：
- 配置文件中 `user_alias` 格式是否正确
- 用户ID是否准确（必须完全匹配）
- 是否重启插件使配置生效

---

### 3. LLM监控超时问题修复 ✅

**问题描述：**
- 用户反馈：建立提醒别人类的日程时，LLM监控没有触发（超时了）
- 但提醒自己的日程可以正常工作

**根本原因：**
在 [main.py](main.py) 第267行，调用 `await self._get_at_target(event)` 时：
- 该方法会调用API获取群成员列表或陌生人信息（第389、399行）
- 这些API调用可能比较慢（尤其是大群或网络波动时）
- 但该调用**未被包含在 `asyncio.wait_for()` 超时保护中**
- 导致整个流程卡在获取@对象这一步，无法继续后续步骤

**修复方案：**
为 `_get_at_target()` 调用添加超时保护：
```python
try:
    target_id, target_name = await asyncio.wait_for(
        self._get_at_target(event),
        timeout=10.0  # 10秒超时
    )
    if target_id:
        logger.info(f"[LLM监控模式] 找到@对象: {target_name} ({target_id})")
        schedule_info["target_id"] = target_id
        schedule_info["target_name"] = target_name
    else:
        logger.info(f"[LLM监控模式] 未找到@对象，默认为自己")
except asyncio.TimeoutError:
    logger.warning(f"[LLM监控模式] 获取@对象超时，默认为自己")
except Exception as e:
    logger.error(f"[LLM监控模式] 获取@对象异常: {e}", exc_info=True)
```

**修改文件：**
- [main.py](main.py) 第265-283行：添加超时保护和异常处理

**效果：**
- 即使获取@对象超时，也不会阻塞整个流程
- 超时时默认为自己，继续创建日程
- 记录警告日志便于后续排查

---

## 技术细节

### 超时策略调整
LLM监控模式的各步骤超时设置：
- 步骤1：判断是否为日程请求 - 10秒
- 步骤2：提取日程信息 - 15秒
- **步骤3（新增）：获取@对象 - 10秒**
- 步骤4：创建日程 - 20秒
- 步骤5：生成确认回复 - 10秒

总计最大超时：65秒（各步骤独立计时）

### API调用优化建议
未来可以考虑：
1. 缓存群成员列表，减少API调用频率
2. 对 `get_group_member_list` 添加单独的超时控制
3. 使用更快的昵称获取方式（如果平台支持）

---

## 升级方法

1. **备份当前版本**（可选）
   ```bash
   cp main.py main.py.v3.2.3.bak
   ```

2. **替换文件**
   - 直接覆盖 `main.py`

3. **重启AstrBot**
   - 重启后插件将自动使用新版本

4. **验证升级**
   - 检查日志中是否有 `[v3.2.4]` 版本标识
   - 测试戳一戳功能是否正常
   - 测试@别人建立日程是否不再超时

---

## 测试建议

### 测试用例1：戳一戳功能
1. 发送：`提醒我 5分钟后 测试戳一戳`
2. 预期：5分钟后收到戳一戳 + 提醒消息
3. 验证：检查是否真的被戳了（不是只有消息）

### 测试用例2：@别人的日程
1. 在群里发送：`提醒 @张三 明天下午2点开会`
2. 预期：
   - 不会超时
   - 立即收到确认消息
   - 明天2点张三会收到戳一戳+提醒
3. 验证：检查日志中"步骤3"是否在10秒内完成

### 测试用例3：昵称映射
1. 配置：
   ```json
   {
     "user_alias": ["你的QQ号,老板"]
   }
   ```
2. 重启插件
3. 发送：`提醒我 1分钟后 测试昵称`
4. 预期：1分钟后收到的消息中包含"老板"而非QQ昵称

### 测试用例4：大群环境
1. 在500+人的大群中
2. 发送：`提醒 @群成员 1分钟后 测试`
3. 预期：即使群成员列表获取较慢，也不会超时

---

## 已知限制

1. **戳一戳仅支持aiocqhttp平台**
   - 其他平台（如微信、Telegram）不支持戳一戳API
   - 会自动跳过并记录日志

2. **昵称获取依赖平台API**
   - 如果平台API不可用，会使用默认名称"TA"
   - 群成员昵称优先使用群名片，其次使用昵称

3. **超时保护的副作用**
   - 获取@对象超时时，会默认为提醒自己
   - 可能导致原本想提醒别人的日程变成提醒自己

---

## 参考资料

- [pokepro插件源码](https://github.com/Zhalslar/astrbot_plugin_pokepro) - 戳一戳功能参考
- [v3.2.3更新说明](v3.2.3_更新说明.md) - 上一版本修复内容
- [v3.2.3深度分析](v3.2.3_深度分析与修复方案.md) - Core/API层问题分析

---

## 贡献者
- GitHub Copilot (Claude Sonnet 4.5) - 代码分析与修复

## 反馈
如有问题，请提供：
1. 完整的错误日志
2. 触发问题的消息内容
3. 当前配置文件（脱敏后）
4. AstrBot和napcat版本信息
