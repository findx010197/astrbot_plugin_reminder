# uni_nickname插件集成测试指南

## 测试前准备

### 1. 确认uni_nickname插件已安装并配置

```bash
# 检查插件是否存在
ls data/plugins/ | grep uni_nickname

# 检查配置文件
cat data/config/uni_nickname.json
```

**预期配置格式：**
```json
{
  "nickname_mappings": [
    "1498976088,sb",
    "其他用户ID,其他昵称"
  ],
  "working_mode": "prompt"
}
```

### 2. 配置昵称映射

使用命令配置（推荐）：
```
/nickname set 1498976088 sb
/nickname list
```

或直接编辑配置文件：
```json
{
  "nickname_mappings": [
    "1498976088,sb"
  ]
}
```

## 测试场景

### 测试1：验证配置读取

**发送任意提醒消息触发插件**（如"提醒我1分钟后测试"）

**关键日志查找：**
```
[昵称映射] 尝试方法1: context.get_plugin_config
[昵称映射] ✓ 方法1成功，读取到配置: ...
或
[昵称映射] ✓ 方法2成功，读取到配置: ...
```

**成功标志：**
```
[昵称映射] ✓ 成功加载 1 个昵称映射: {'1498976088': 'sb'}
```

**失败情况：**
```
[昵称映射] ⚠️ 未找到uni_nickname插件配置（两种方法都失败）
```

**解决方法：**
- 确认uni_nickname插件已安装
- 检查配置文件路径：`data/config/uni_nickname.json`
- 确认配置文件格式正确

---

### 测试2：基于昵称的提醒（核心功能）

**消息：** `axx一分钟后提醒sb起床`

**预期LLM提取：**
```json
{
  "time": "一分钟后",
  "event": "起床",
  "target": "sb"
}
```

**关键日志：**
```
[LLM监控模式] 尝试根据昵称查找用户: 'sb'
[昵称反查] 查找昵称: 'sb', 可用映射: {'1498976088': 'sb'}
[昵称反查] ✓ 找到匹配: 'sb' → 1498976088
[LLM监控模式] ✓ 找到昵称映射: 'sb' → 用户ID=1498976088
```

**成功标志：**
- 日程创建成功
- target_id = 1498976088
- 1分钟后收到提醒

**失败情况1：LLM未提取到昵称**
```
{
  "target": "用户"  // ← 错误
}
```
**原因：** LLM没有识别到"sb"是昵称
**解决：** 检查LLM prompt中是否包含昵称列表提示

**失败情况2：昵称反查失败**
```
[昵称反查] ✗ 未找到匹配: 'sb'
```
**原因：** uni_nickname配置未加载或昵称不匹配
**解决：** 检查测试1的配置读取日志

---

### 测试3：提醒自己（验证昵称显示）

**消息：** `axx一分钟后提醒我上厕所`

**关键日志：**
```
[获取昵称] ✓ 用户1498976088使用uni_nickname映射: 'sb'
```

**提醒消息预期：**
```
@你 起床的时间到了
```

**检查点：**
1. 提醒消息中是否@正确
2. 昵称是否来自uni_nickname（而不是群昵称）
3. 没有重复显示昵称

**失败情况：使用了群昵称而非uni_nickname**
```
[获取昵称] 用户1498976088未在uni_nickname中，使用默认名称: '群昵称'
```
**原因：** uni_nickname配置未正确加载

---

### 测试4：消息去重

**连续发送相同消息3次：**
```
axx一分钟后提醒我测试
axx一分钟后提醒我测试
axx一分钟后提醒我测试
```

**预期日志：**
```
[LLM监控模式] ✓ 消息去重检查通过: 1498976088:xxxxx
[LLM监控模式] 关键词预匹配成功: ...
// 第一次正常处理

[LLM监控模式] ⚠️ 检测到重复消息（0.1秒前已处理），跳过: ...
// 第二次被拦截

[LLM监控模式] ⚠️ 检测到重复消息（0.2秒前已处理），跳过: ...
// 第三次被拦截
```

**成功标志：** 只创建1个日程，后续消息被去重拦截

**失败情况：创建了多个相同日程**
- 检查去重缓存是否正常工作
- 可能有多个插件实例在运行

---

## 完整日志模板（成功案例）

```
# 1. 配置读取
[昵称映射] 尝试方法1: context.get_plugin_config
[昵称映射] ✓ 方法1成功，读取到配置: ['nickname_mappings']
[昵称映射] 原始映射列表: ['1498976088,sb']
[昵称映射] ✓ 成功加载 1 个昵称映射: {'1498976088': 'sb'}

# 2. 消息处理
[LLM监控模式] ✓ 消息去重检查通过: 1498976088:12345678
[LLM监控模式] 关键词预匹配成功: axx一分钟后提醒sb起床
[LLM监控模式] 步骤1: 开始判断是否为日程请求...
[LLM监控模式] 步骤1完成: 耗时0.05秒, 结果=True

# 3. 信息提取
[LLM监控模式] 步骤2: 开始提取日程信息...
[LLM监控模式] 步骤2完成: 耗时2.5秒, 提取结果: {'time': '一分钟后', 'event': '起床', 'target': 'sb', ...}

# 4. 昵称查找
[LLM监控模式] 步骤3: 检查@对象...
[LLM监控模式] 尝试根据昵称查找用户: 'sb'
[昵称反查] 查找昵称: 'sb', 可用映射: {'1498976088': 'sb'}
[昵称反查] ✓ 找到匹配: 'sb' → 1498976088
[LLM监控模式] ✓ 找到昵称映射: 'sb' → 用户ID=1498976088

# 5. 创建日程
[LLM监控模式] 步骤4: 开始创建日程...
已创建日程 [R0001] 起床, 触发时间: 02/02 15:30

# 6. 生成确认
[LLM监控模式] 步骤5: 生成确认回复...
→ 发送: "好的，我会在15:30提醒sb起床"

# 7. 触发提醒（1分钟后）
[获取昵称] ✓ 用户1498976088使用uni_nickname映射: 'sb'
→ 发送: @sb 起床的时间到了
```

---

## 常见问题排查

### 问题1：配置读取失败

**症状：**
```
[昵称映射] ⚠️ 未找到uni_nickname插件配置（两种方法都失败）
```

**排查步骤：**
1. 检查插件是否安装：`ls data/plugins/astrbot_plugin_uni_nickname`
2. 检查配置文件：`cat data/config/uni_nickname.json`
3. 检查配置格式是否正确（JSON格式）
4. 尝试重启AstrBot

---

### 问题2：LLM未识别昵称

**症状：**
```
提取结果: {'target': '用户'}  // 应该是 {'target': 'sb'}
```

**原因：**
- LLM prompt中未包含昵称列表
- 昵称太短或太特殊，LLM无法识别

**解决：**
1. 检查`_extract_schedule_info`方法中的prompt
2. 确认prompt包含：`已知昵称列表：sb, ...`
3. 尝试使用更明确的昵称（如"小龙"而非"sb"）

---

### 问题3：昵称反查失败

**症状：**
```
[昵称反查] ✗ 未找到匹配: 'sb'
可用映射: {}
```

**原因：** 配置未正确加载

**排查：**
1. 检查测试1的配置读取日志
2. 确认映射列表不为空
3. 检查用户ID是否完全匹配（包括空格）

---

### 问题4：使用了群昵称而非uni_nickname

**症状：**
```
[获取昵称] 用户1498976088未在uni_nickname中，使用默认名称: '群昵称'
```

**原因：** 配置读取失败或用户ID不匹配

**解决：**
1. 重新测试配置读取（测试1）
2. 确认配置中的用户ID完全正确
3. 检查是否有多余的空格或特殊字符

---

### 问题5：消息重复处理

**症状：** 同一条消息触发了多次，创建了多个日程

**原因：**
- 去重缓存未生效
- 有多个插件实例在运行

**排查：**
```
# 检查是否有重复消息日志
grep "检测到重复消息" logs/

# 检查插件实例数
grep "日程提醒插件配置加载完成" logs/
```

---

## 测试检查清单

- [ ] uni_nickname插件已安装
- [ ] 配置文件存在且格式正确
- [ ] 昵称映射配置完成（至少1个）
- [ ] 测试1：配置读取成功
- [ ] 测试2：基于昵称的提醒成功
- [ ] 测试3：提醒消息使用uni_nickname昵称
- [ ] 测试4：消息去重正常工作
- [ ] 日志中无ERROR级别错误
- [ ] 提醒消息中昵称无重复显示

---

## 调试技巧

### 1. 实时查看日志
```bash
tail -f logs/astrbot.log | grep "昵称"
```

### 2. 过滤关键日志
```bash
# 查看昵称映射相关
grep "\[昵称" logs/astrbot.log

# 查看LLM监控流程
grep "\[LLM监控模式\]" logs/astrbot.log

# 查看错误
grep "ERROR" logs/astrbot.log | grep reminder
```

### 3. 验证uni_nickname工作状态
```bash
# 在群里发送
/nickname list

# 预期看到配置的映射列表
```

### 4. 手动验证配置文件
```bash
# 读取配置
cat data/config/uni_nickname.json

# 格式化查看
python -m json.tool data/config/uni_nickname.json
```

---

## 成功案例参考

**环境：**
- AstrBot 4.13.2
- astrbot_plugin_reminder v3.2.7
- astrbot_plugin_uni_nickname v1.1.0

**配置：**
```json
{
  "nickname_mappings": ["1498976088,sb"],
  "working_mode": "prompt"
}
```

**测试结果：**
✓ 配置读取成功
✓ 基于昵称的提醒成功创建
✓ 提醒消息使用正确昵称
✓ 消息去重正常工作
✓ 无ERROR日志
