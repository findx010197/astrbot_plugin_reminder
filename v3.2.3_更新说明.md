# v3.2.3 更新说明 - 彻底修复@成员提醒卡死问题

## 📅 发布日期
2026年2月2日

## 🎯 核心修复

### 问题回顾
**症状**：
- ✅ 提醒自己（不@成员）：正常工作
- ❌ 提醒别人（@成员）：30秒卡死，AstrBot控制界面冻结

**根本原因**：
经过深入分析发现，问题源于**导入层和At组件使用错误**：

1. **错误的导入**：
   ```python
   # 旧代码（错误）
   import astrbot.api.message_components as Comp
   chain.append(Comp.At(qq=schedule.target_id))  # API层的At类
   ```
   
2. **组件不兼容**：
   - `astrbot.api.message_components.At` (API层抽象) ≠ `astrbot.core.message.components.At` (Core层实现)
   - API层的At组件可能不支持qq参数或有不同的初始化方式
   - 导致消息格式异常，aiocqhttp/napcat发送失败，触发30秒超时

3. **缺少真实昵称**：
   - 旧的`_get_at_target`只返回ID，昵称固定为"TA"
   - 没有调用QQ协议API获取真实昵称

## ✨ v3.2.3 修复内容

### 🔴 核心修复

#### 1. 修复导入层
**修改前**：
```python
import astrbot.api.message_components as Comp
```

**修改后**：
```python
from astrbot.core.message.components import Plain, At, Record
```

**影响**：现在使用正确的Core层组件，确保消息格式兼容

#### 2. 修复At组件构造
**修改前**：
```python
chain.append(Comp.At(qq=schedule.target_id))  # target_id是字符串
```

**修改后**：
```python
chain.append(At(qq=int(schedule.target_id)))  # 使用int类型
```

**影响**：At组件现在使用正确的int类型参数

#### 3. 重写_get_at_target函数
**修改前**（同步函数）：
```python
def _get_at_target(self, event: AstrMessageEvent) -> tuple[Optional[str], Optional[str]]:
    for component in event.message_obj.message:
        if isinstance(component, Comp.At):  # 检查错误的At类
            target_id = str(getattr(component, "qq", ""))
            if target_id:
                return target_id, "TA"  # 固定昵称
    return None, None
```

**修改后**（异步函数）：
```python
async def _get_at_target(self, event: AstrMessageEvent) -> tuple[Optional[str], Optional[str]]:
    # 1. 提取At组件
    target_id = None
    for component in event.message_obj.message:
        if isinstance(component, At):  # 检查正确的At类
            target_id = str(getattr(component, "qq", ""))
            break
    
    if not target_id:
        return None, None
    
    # 2. 获取真实昵称（仅aiocqhttp平台）
    target_name = "TA"
    if isinstance(event, AiocqhttpMessageEvent):
        group_id = event.get_group_id()
        if group_id:  # 群聊
            members = await event.bot.api.call_action('get_group_member_list', group_id=group_id)
            for m in members:
                if str(m.get("user_id")) == target_id:
                    target_name = m.get("card") or m.get("nickname")
                    break
        else:  # 私聊
            info = await event.bot.api.call_action('get_stranger_info', user_id=int(target_id))
            target_name = info.get("nickname", "TA")
    
    return target_id, target_name
```

**影响**：
- 异步获取真实昵称
- 群聊环境：获取群名片或昵称
- 私聊环境：获取用户昵称
- 所有调用处改为`await self._get_at_target(event)`

### ✨ 新功能：戳一戳

#### 1. 添加_send_poke方法
```python
async def _send_poke(self, unified_msg_origin: str, target_id: str):
    """发送戳一戳（仅aiocqhttp平台）"""
    # 解析平台信息
    # 调用napcat API: group_poke / friend_poke
```

**支持平台**：
- ✅ QQ (通过aiocqhttp/napcat)
- ❌ 其他平台（会自动跳过）

#### 2. 完整提醒流程
**旧流程**：
1. @提醒对象+文本
2. TTS语音（可选）

**新流程（v3.2.3）**：
1. **戳一戳提醒对象** ← 新增
2. @提醒对象+真实昵称+文本
3. TTS语音（可选）

**代码示例**：
```python
async def _send_reminder(self, schedule: ScheduleItem):
    # 步骤1: 戳一戳
    if schedule.target_id and self.config.get("enable_poke", True):
        await self._send_poke(umo, schedule.target_id)
        await asyncio.sleep(0.5)  # 短暂延迟
    
    # 步骤2: @+文本
    chain = []
    if schedule.target_id:
        chain.append(At(qq=int(schedule.target_id)))
        chain.append(Plain(" "))
    chain.append(Plain(message))
    
    # 步骤3: TTS语音
    if self.config.get("enable_tts", False):
        await self._send_tts_reminder(schedule, message)
```

#### 3. 新增配置项
在`_conf_schema.json`中添加：
```json
{
  "enable_poke": {
    "description": "启用戳一戳提醒",
    "type": "bool",
    "hint": "是否在提醒时使用戳一戳功能（仅QQ平台支持）",
    "default": true
  }
}
```

## 🔧 其他优化

### 1. 异步调用修正
所有调用`_get_at_target`的地方都改为异步：
```python
# 旧代码
target_id, target_name = self._get_at_target(event)

# 新代码
target_id, target_name = await self._get_at_target(event)
```

**影响位置**：
- `on_message` (LLM监控模式)
- `handle_every_command` (循环日程指令)
- `handle_at_command` (at指令)
- `handle_remind_command` (remind指令)

### 2. 增强日志记录
```python
logger.info(f"获取到群成员昵称: {target_name} (ID:{target_id})")
logger.info(f"发送戳一戳: target={schedule.target_id}")
logger.info(f"已发送提醒消息: {message[:50]}...")
```

## 📊 测试建议

### 测试用例

#### 1. 提醒自己
```
用户: axx，10分钟后提醒我喝水
预期: ✅ 正常创建，无卡死
```

#### 2. 提醒别人（群聊）
```
用户: axx，一分钟后提醒 @张三 吃饭
预期: 
✅ 无卡死（不再超时30秒）
✅ 戳一戳张三
✅ @张三 昵称显示正确（不再是"TA"）
✅ 提醒消息发送成功
```

#### 3. 提醒别人（私聊）
```
用户: /callme at 1分钟后 @李四 开会
预期:
✅ 昵称显示正确
✅ 私聊戳一戳（如果支持）
```

#### 4. 循环提醒
```
用户: /callme every day 9:00 提醒 @王五 晨会
预期:
✅ 每天9点触发
✅ 每次都戳一戳+@
```

### 错误场景测试
1. LLM超时：应有兜底回复
2. 网络断开：应记录错误日志
3. 非QQ平台：戳一戳自动跳过
4. 无@对象：正常提醒自己

## 🚨 已知限制

### 戳一戳功能
当前实现有一个限制：
```python
# TODO: 完善bot实例获取逻辑
logger.warning("戳一戳功能尚未完全实现，需要进一步测试和完善")
```

**原因**：
- `_send_poke`中没有`event`对象，无法直接访问`bot`实例
- 需要通过`unified_msg_origin`重建会话信息
- 需要访问`platform.get_client()`来调用API

**临时方案**：
- 代码已准备就绪，仅需补充bot实例获取逻辑
- 可在实际测试中根据AstrBot版本调整

**建议**：
1. 在数据库中增加`platform_name`字段
2. 或在`_create_schedule`时存储`event`的更多信息
3. 或通过`Context`的`platform_manager`获取活跃bot

## 📦 更新方式

### 方式1：Git拉取（推荐）
```bash
cd /path/to/astrbot_plugin_reminder
git pull origin main
git checkout v3.2.3
# 重启AstrBot
```

### 方式2：覆盖文件
下载以下文件并覆盖：
- `main.py`
- `metadata.yaml`
- `_conf_schema.json`
- `CHANGELOG.md`

### 方式3：插件市场（等待审核）
等待插件市场更新到v3.2.3

## 📝 升级后配置检查

升级后，请在AstrBot管理面板检查：
1. **enable_poke**：是否启用戳一戳（默认true）
2. **enable_llm_monitor**：是否启用LLM监控（默认true）
3. **enable_tts**：是否启用TTS语音（默认false）

## 🔗 相关资源

- **插件仓库**：https://github.com/findx010197/astrbot_plugin_reminder
- **Issue反馈**：https://github.com/findx010197/astrbot_plugin_reminder/issues
- **参考插件**：[AtTool插件](https://github.com/nuomicici/astrbot_plugin_AtTool)
- **Napcat API**：https://napcat.apifox.cn/

## 💬 反馈与支持

如果遇到问题，请提供：
1. AstrBot版本
2. 插件版本（v3.2.3）
3. 平台类型（aiocqhttp/其他）
4. 完整错误日志
5. 复现步骤

**提交Issue**：https://github.com/findx010197/astrbot_plugin_reminder/issues/new

---

**感谢使用astrbot_plugin_reminder！** 🎉
