# v3.2.5 修复说明

## 发布时间
2026-02-02

## 修复问题

### 1. 提醒消息结构修复 ✅

**问题描述：**
用户反馈提醒消息中没有使用配置的昵称映射信息。

**用户期望的结构：**
```
戳一戳 + @目标成员 + 目标成员昵称（固定文本） + 提醒文本（LLM生成）
```

**v3.2.4的问题：**
- 消息结构：`戳一戳 + @目标成员 + LLM生成的文本`
- 昵称只在LLM生成的文本中提及，没有作为独立的固定文本显示
- 配置的昵称映射只影响LLM生成内容，不会作为固定文本出现

**v3.2.5的修复：**
```python
# main.py 第1267-1278行
if schedule.target_id:
    chain.append(At(qq=int(schedule.target_id)))  # @用户
    chain.append(Plain(" "))  # 空格
    
    # 添加固定的昵称文本（使用配置映射）
    target_display = self._get_user_display_name(
        schedule.target_id, schedule.target_name
    )
    chain.append(Plain(f"{target_display} "))  # 昵称+空格

chain.append(Plain(message))  # LLM生成的提醒内容
```

**效果示例：**
假设配置中设置：`"user_alias": ["123456789,老板"]`

提醒触发时会发送：
```
（戳一戳123456789）
@老板 老板 是小助手拜托我提醒您开会的哦~
```

结构分解：
- 戳一戳：自动发送
- @老板：At组件
- 老板：固定文本（来自配置映射）
- 是小助手拜托我...：LLM生成的提醒内容

---

### 2. LLM监控识别@别人提醒的优化 ✅

**问题描述：**
用户反馈"提醒别人"这一类日程无法被LLM监控检测到。

**根本原因分析：**

1. **关键词匹配问题**：
   - 消息格式：`"提醒 @某人 明天开会"` 或 `"提醒 [CQ:at,qq=123] 明天开会"`
   - @符号或CQ码可能分隔关键词，导致匹配失败

2. **LLM提取信息不准确**：
   - LLM可能把"@某人"当作事件内容的一部分
   - 未明确告知LLM消息中包含@表示提醒别人

**v3.2.5的修复：**

#### 修复1：改进关键词预过滤（第174-188行）
```python
# 移除@符号和CQ码后再检查，避免它们干扰关键词匹配
clean_msg = re.sub(r'\[CQ:.*?\]', '', message_str)  # 移除CQ码
clean_msg = re.sub(r'@\S+', '', clean_msg)  # 移除@提及
has_trigger_keyword = any(
    keyword in message_str or keyword in clean_msg 
    for keyword in trigger_keywords
)
```

**效果**：
- 原消息：`"提醒 @张三 明天开会"`
- clean_msg：`"提醒  明天开会"`（@张三被移除）
- 关键词"提醒"可以正确匹配

#### 修复2：改进 `_is_schedule_trigger` 方法（第420-443行）
```python
# 移除CQ码和@符号后再检查
clean_msg = re.sub(r'\[CQ:.*?\]', '', message)
clean_msg = re.sub(r'@\S+', '', clean_msg)

if any(keyword in message or keyword in clean_msg for keyword in trigger_keywords):
    logger.info(f"通过关键词匹配触发日程设定: {message}")
    return True
```

#### 修复3：改进LLM提取信息的提示词（第460-496行）
```python
# 检查消息中是否有@（At组件或文本中的@）
has_at = False
for component in event.message_obj.message:
    if isinstance(component, At):
        has_at = True
        break
if not has_at and '@' in message:
    has_at = True

at_hint = ""
if has_at:
    at_hint = "\n注意：消息中包含@某人，这表示用户想提醒另一个人，而不是提醒自己。target字段应填写被@的人（如果无法确定名字，填'被@的人'）。"

prompt = f"""请从以下用户消息中提取日程关键信息...
{at_hint}
...
3. target: 提醒的目标对象（如果消息中有@某人或明确提到提醒别人，填写被提醒者；否则填"用户"表示提醒自己）
"""
```

**效果**：
- 检测到消息中有@或At组件
- 自动在提示词中添加提示："消息中包含@某人，这表示用户想提醒另一个人"
- LLM能正确识别这是提醒别人的请求

---

## 测试场景

### 场景1：测试昵称映射显示

**配置**：
```json
{
  "user_alias": [
    "你的QQ号,老板",
    "朋友的QQ号,小李"
  ]
}
```

**测试步骤**：
1. 发送：`提醒 @小李 5分钟后开会`
2. 等待5分钟
3. 检查小李收到的消息结构

**预期结果**：
```
（先收到戳一戳）
@小李 小李 是老板拜托我提醒您开会的哦~
```

**验证点**：
- ✓ 有戳一戳
- ✓ 有@小李（At组件）
- ✓ 有固定文本"小李"（来自配置）
- ✓ 提醒内容提到"老板"（发送者昵称）

---

### 场景2：测试LLM监控识别@别人

**测试步骤1**：纯文本@
```
发送：提醒 @张三 明天下午2点开会
预期：立即收到确认消息（不超时）
日志：[LLM监控模式] 通过关键词匹配触发日程设定
```

**测试步骤2**：QQ的At组件
```
发送：提醒 @群成员 1小时后吃饭
预期：立即收到确认消息
日志：[LLM监控模式] 找到@对象: 群成员 (12345)
```

**测试步骤3**：复杂场景
```
发送：帮我提醒 @小王 和 @小李 后天上午9点开会
预期：能识别为日程请求（至少提醒第一个@的人）
```

**验证点**：
- ✓ 关键词"提醒"被正确识别（即使有@）
- ✓ LLM提取信息时识别出是提醒别人
- ✓ 正确获取@对象的ID和昵称
- ✓ 不会超时或卡死

---

### 场景3：对比测试（自己 vs 别人）

**提醒自己**：
```
发送：提醒我明天开会
等待触发
收到：@你 你 明天开会的时间到了哦~
```

**提醒别人**：
```
发送：提醒 @小张 明天开会
等待触发
小张收到：@小张 小张 是你拜托我提醒您开会的哦~
```

---

## 技术细节

### 消息结构变更对比

**v3.2.4**:
```python
chain = [
    At(qq=int(target_id)),
    Plain(" "),
    Plain(llm_message)  # "是老板拜托我提醒小李开会的哦~"
]
```

**v3.2.5**:
```python
chain = [
    At(qq=int(target_id)),
    Plain(" "),
    Plain("小李 "),  # 固定昵称文本（来自配置映射）
    Plain(llm_message)  # "是老板拜托我提醒您开会的哦~"
]
```

### 关键词匹配逻辑

**处理流程**：
1. 原消息：`"提醒 @张三 明天开会"`
2. 清理后：`"提醒  明天开会"`（移除@张三）
3. 匹配：`"提醒" in 原消息 or "提醒" in 清理后` → True
4. 触发LLM判断和信息提取

**覆盖的格式**：
- `提醒 @某人 ...`
- `提醒 [CQ:at,qq=123] ...`
- `@某人 提醒 ...`（关键词在@之后）
- `帮我提醒 @xxx ...`

---

## 已知限制

### 1. 多人@的处理
当前只能识别第一个@对象：
```
消息：提醒 @小王 @小李 明天开会
处理：只提醒小王
建议：创建两条日程，分别@每个人
```

### 2. 昵称重复显示
如果LLM生成的内容中也包含昵称，会显示两次：
```
@小李 小李 小李，是老板拜托我提醒您...
       ↑固定 ↑LLM生成
```
解决：优化LLM提示词，让其避免重复昵称

### 3. 配置未生效的排查
如果昵称映射不生效：
- 检查配置格式：`["用户ID,昵称"]`（注意逗号和引号）
- 用户ID必须完全匹配（QQ号）
- 重启插件使配置生效
- 查看日志：搜索 `_get_user_display_name`

---

## 升级方法

1. 拉取最新代码
2. 重启AstrBot
3. 测试提醒功能

## 回滚方案

如有问题，回滚到v3.2.4：
```bash
git checkout v3.2.4
# 重启AstrBot
```

---

## 后续优化建议

1. **支持多人@**：
   - 检测消息中所有At组件
   - 为每个@对象创建独立的日程

2. **智能昵称去重**：
   - LLM提示词中明确要求避免重复昵称
   - 或在固定文本和LLM文本之间做去重检测

3. **配置界面优化**：
   - 在AstrBot管理界面添加"昵称映射管理"功能
   - 支持可视化添加/删除映射

4. **消息格式可配置**：
   - 允许用户自定义提醒消息结构
   - 例如：`{poke}{at}{nickname}{message}` 或 `{poke}{at}{message}`

---

## 参考

- 用户反馈：提醒文本结构应为"戳一戳+@+昵称+提醒文本"
- 用户反馈：LLM监控无法识别"提醒别人"类型的日程
- 修复commit: v3.2.5
