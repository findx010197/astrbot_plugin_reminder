# v3.2.7 集成uni_nickname插件 - 修改清单

## 📅 更新时间
2026-02-02

## 🎯 目标
解决两个核心问题：
1. 昵称重复显示（提醒插件 + uni_nickname插件重复维护昵称）
2. @成员提醒LLM监控失效（30秒超时）

## 🔧 代码修改

### 1. main.py

#### 1.1 新增昵称映射获取方法（第70-135行）
```python
def _get_uni_nickname_mappings(self) -> dict:
    """获取uni_nickname插件的昵称映射（QQ号→昵称）"""
    # 尝试两种方式读取配置：
    # 方法1：通过context.get_plugin_config()
    # 方法2：直接读取配置文件 data/config/uni_nickname.json
    
    # 解析格式：["用户ID,昵称"] → {"用户ID": "昵称"}
    # 返回映射字典
```

#### 1.2 新增反向查找方法（第137-151行）
```python
def _get_userid_by_nickname(self, nickname: str) -> Optional[str]:
    """根据昵称查找用户ID（反向查询）"""
    # 遍历映射，匹配昵称
    # 返回对应的用户QQ号
```

#### 1.3 修改_get_user_display_name方法（第1705-1716行）
**修改前：**
```python
def _get_user_display_name(self, user_id: str, default_name: str) -> str:
    """获取用户显示名称（优先使用配置的别名）"""
    user_alias_list = self.config.get("user_alias", [])
    for item in user_alias_list:
        # ... 遍历user_alias配置
    return default_name
```

**修改后：**
```python
def _get_user_display_name(self, user_id: str, default_name: str) -> str:
    """获取用户显示名称（优先使用uni_nickname插件的映射）"""
    mappings = self._get_uni_nickname_mappings()
    return mappings.get(str(user_id), default_name)
```

#### 1.4 修改LLM提取提示词（第559-583行）
**添加昵称提示：**
```python
# 获取uni_nickname的昵称列表，用于LLM识别
nickname_mappings = self._get_uni_nickname_mappings()
nicknames_list = list(nickname_mappings.values())
nickname_hint = f"\n已知昵称列表：{', '.join(nicknames_list[:10])}"

# 更新target字段说明：
"""
3. target: 提醒的目标对象
   - 如果消息中提到具体人名/昵称（如"提醒小龙"、"提醒老板"），提取该人名/昵称
   - 如果消息中有@提及，提取被@的人的显示名称
   - 否则填"用户"表示提醒自己
"""
```

#### 1.5 修改LLM监控处理逻辑（第355-383行）
**增强@对象检查：**
```python
# 检查是否有 @ 对象
target_id, target_name = await self._get_at_target(event)
if target_id:
    # 使用@对象
    schedule_info["target_id"] = target_id
    schedule_info["target_name"] = target_name
else:
    # 如果没有@对象，检查LLM提取的target是否为昵称
    extracted_target = schedule_info.get("target", "用户")
    if extracted_target != "用户":
        # 尝试根据昵称查找用户ID
        nickname_user_id = self._get_userid_by_nickname(extracted_target)
        if nickname_user_id:
            schedule_info["target_id"] = nickname_user_id
            schedule_info["target_name"] = extracted_target
```

#### 1.6 修改提醒消息格式（第1374-1383行）
**移除固定昵称文本：**

**修改前：**
```python
if schedule.target_id:
    chain.append(At(qq=int(schedule.target_id)))
    chain.append(Plain(" "))
    
    # 添加固定的昵称文本
    target_display = self._get_user_display_name(...)
    chain.append(Plain(f"{target_display} "))

chain.append(Plain(message))
```

**修改后：**
```python
# 昵称替换由uni_nickname插件在LLM层面处理，这里不需要添加固定昵称文本
if schedule.target_id:
    chain.append(At(qq=int(schedule.target_id)))
    chain.append(Plain(" "))

chain.append(Plain(message))
```

#### 1.7 更新版本号（第38-43行）
```python
@register(
    "astrbot_plugin_reminder",
    "findx010197",
    "智能日程提醒插件，支持LLM监控与指令双模式，循环日程，戳一戳，集成uni_nickname昵称映射",
    "3.2.7",  # 版本从3.2.3更新到3.2.7
    "https://github.com/findx010197/astrbot_plugin_reminder",
)
```

### 2. _conf_schema.json

#### 2.1 移除user_alias配置项（第137-143行）
**删除：**
```json
"user_alias": {
  "description": "用户ID与称呼映射表",
  "type": "list",
  "hint": "每一项格式为：用户ID,称呼。例如：123456789,老板",
  "default": []
}
```

### 3. metadata.yaml

#### 3.1 更新描述和版本
```yaml
desc: 智能日程提醒插件，支持LLM监控与指令双模式创建日程，基于人格生成提醒话语，支持TTS语音播报，支持循环日程（每天/每周/每月），支持戳一戳提醒，使用SQLite数据库存储。已集成uni_nickname插件，支持基于昵称的提醒。
version: v3.2.7
```

### 4. README.md

#### 4.1 添加依赖说明
```markdown
## ⚠️ 重要依赖

**本插件依赖于 [uni_nickname插件](https://github.com/Hakuin123/astrbot_plugin_uni_nickname) 来管理用户昵称。**
```

#### 4.2 更新使用示例
```markdown
### 基于昵称的提醒（推荐新方式）✨
用户: 提醒 小龙 明天早上开会
Bot: 好的，我会在明天早上8点提醒小龙开会哦~
```

## 📊 工作流程

### 用户使用昵称提醒的完整流程

```
1. 用户输入
   ↓
   "axx提醒 小龙 明天开会"

2. 关键词预匹配
   ↓
   发现"提醒"关键词 → 触发LLM监控

3. LLM判断
   ↓
   确认为日程设定请求

4. LLM提取信息
   ↓
   {
     "time": "明天",
     "event": "开会",
     "target": "小龙"  ← 提取到昵称
   }

5. 检查@对象
   ↓
   没有@组件

6. 昵称反向查找
   ↓
   _get_userid_by_nickname("小龙")
   → 返回 "3342496519"

7. 创建日程
   ↓
   schedule_info = {
     "target_id": "3342496519",
     "target_name": "小龙",
     ...
   }

8. 生成确认回复
   ↓
   "好的，我会在明天早上8点提醒小龙开会哦~"

9. 到时触发提醒
   ↓
   消息链: [At(3342496519), Plain(" "), Plain("开会的时间到了")]
   → uni_nickname在LLM层面替换昵称
   → 最终显示: @小龙 开会的时间到了
```

## 🔍 关键技术点

### 1. uni_nickname配置读取
- 优先通过`context.get_plugin_config()`获取
- 降级读取`data/config/uni_nickname.json`文件
- 容错处理：配置不存在时返回空字典

### 2. 昵称映射格式
**uni_nickname配置：**
```json
{
  "nickname_mappings": [
    "3342496519,小龙",
    "123456789,老板"
  ]
}
```

**解析后的字典：**
```python
{
  "3342496519": "小龙",
  "123456789": "老板"
}
```

### 3. 反向查找优化
```python
def _get_userid_by_nickname(self, nickname: str) -> Optional[str]:
    mappings = self._get_uni_nickname_mappings()
    # 线性查找，时间复杂度O(n)
    # 如果昵称映射很多（>100），可以考虑建立反向索引缓存
    for user_id, nick in mappings.items():
        if nick == nickname:
            return user_id
    return None
```

### 4. LLM提示词优化
**提供已知昵称列表：**
- 让LLM知道哪些是有效昵称
- 最多显示前10个昵称（避免prompt过长）
- 格式：`已知昵称列表：小龙, 老板, 小明...`

### 5. 消息格式优化
**之前：**
```
[At(3342496519), Plain(" "), Plain("小龙 "), Plain("开会的时间到了")]
                                    ↑↑ 固定昵称文本（重复）
```

**现在：**
```
[At(3342496519), Plain(" "), Plain("开会的时间到了")]
                             ↑ uni_nickname会在LLM层面处理昵称
```

## 🐛 已知问题和解决方案

### 问题1：uni_nickname未安装
**表现：**
- 日志显示：`[昵称映射] 未找到uni_nickname插件配置`
- 昵称提醒功能不生效

**解决：**
- 安装uni_nickname插件
- 配置昵称映射

### 问题2：昵称不匹配
**表现：**
- 日志显示：`[LLM监控模式] 未找到昵称映射: xxx`
- 提醒创建成功但是提醒自己而非目标用户

**解决：**
- 确认昵称拼写与配置完全一致
- 使用`/nickname list`查看已配置的昵称

### 问题3：性能考虑
**昵称映射数量较多时：**
- 每次调用都重新读取配置
- 可以在ReminderPlugin中添加缓存机制
- 定期刷新缓存（如每5分钟）

**优化建议：**
```python
class ReminderPlugin(Star):
    def __init__(self, context: Context):
        # ...
        self._nickname_cache = {}
        self._nickname_cache_time = 0
        self._cache_ttl = 300  # 5分钟
    
    def _get_uni_nickname_mappings(self) -> dict:
        now = time.time()
        if now - self._nickname_cache_time < self._cache_ttl:
            return self._nickname_cache
        
        # 读取配置...
        self._nickname_cache = mappings
        self._nickname_cache_time = now
        return mappings
```

## ✅ 测试清单

### 功能测试
- [ ] 昵称提醒创建：`提醒 小龙 1分钟后 测试`
- [ ] @提醒创建：`提醒 @小龙 1分钟后 测试`
- [ ] 自己提醒：`提醒我 1分钟后 测试`
- [ ] 昵称不存在：`提醒 不存在的人 1分钟后 测试`（应该提示错误或提醒自己）

### 配置读取测试
- [ ] uni_nickname已安装：正常读取昵称映射
- [ ] uni_nickname未安装：不崩溃，返回空字典
- [ ] 配置文件格式错误：捕获异常，记录日志

### 消息格式测试
- [ ] 提醒消息不包含重复昵称
- [ ] @组件正常工作
- [ ] uni_nickname的昵称替换正常

### 日志测试
- [ ] `[昵称映射] 成功加载 N 个昵称映射`
- [ ] `[LLM监控模式] 尝试根据昵称查找用户: xxx`
- [ ] `[LLM监控模式] 找到昵称对应的用户: xxx → yyy`
- [ ] `[LLM监控模式] 未找到昵称映射: xxx`

## 📦 发布准备

### 文件清单
- [x] main.py - 核心代码修改
- [x] _conf_schema.json - 移除user_alias
- [x] metadata.yaml - 更新版本和描述
- [x] README.md - 添加依赖说明
- [x] v3.2.7更新说明.md - 详细更新文档
- [x] v3.2.7修改清单.md - 本文件

### Git提交
```bash
# 查看修改
git status

# 添加文件
git add main.py _conf_schema.json metadata.yaml README.md
git add v3.2.7更新说明.md v3.2.7修改清单.md

# 提交
git commit -m "v3.2.7: 集成uni_nickname插件，支持基于昵称的提醒

- 移除内置user_alias配置
- 添加uni_nickname配置读取方法
- 实现昵称反向查找
- 增强LLM提取能力，支持昵称识别
- 优化提醒消息格式，移除重复昵称
- 修复@成员提醒LLM监控超时问题
"

# 打标签
git tag v3.2.7

# 推送
git push origin main --tags
```

### 发布说明
```markdown
## v3.2.7 - uni_nickname插件集成

### ✨ 新特性
- 集成uni_nickname插件，支持基于昵称的提醒
- 支持"提醒 小龙 xxx"格式（推荐）

### 🐛 修复
- 修复昵称重复显示问题
- 修复@成员提醒LLM监控超时问题

### 🗑️ 移除
- 移除内置user_alias配置（请迁移到uni_nickname插件）

### ⚠️ 破坏性变更
- 需要安装uni_nickname插件作为依赖
- 旧的user_alias配置将被忽略

### 📖 文档
- 详细更新说明：[v3.2.7更新说明.md](v3.2.7更新说明.md)
- 迁移指南见更新说明文档
```

## 🙏 致谢
感谢 [@Hakuin123](https://github.com/Hakuin123) 开发的 [uni_nickname插件](https://github.com/Hakuin123/astrbot_plugin_uni_nickname)
