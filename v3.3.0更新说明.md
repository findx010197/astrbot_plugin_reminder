# v3.3.0 更新说明 - 简化与稳定

**发布日期：** 2026-02-02

## 🎯 更新理念

v3.2.7尝试集成uni_nickname插件遇到了很多问题。经过深思熟虑，我们决定**回归简单可靠**的实现方式：
- ✅ 专注核心功能："提醒自己"
- ✅ 使用内置昵称配置，易于管理
- ✅ 减少外部依赖，提高稳定性
- ✅ 清晰的消息格式

## 🔄 主要变更

### 1. 功能简化

**移除的功能：**
- ❌ uni_nickname插件集成
- ❌ "提醒别人"功能（基于昵称或@）
- ❌ 复杂的昵称反向查找逻辑
- ❌ LLM提取中的@对象检测

**保留的核心功能：**
- ✅ 提醒自己（LLM监控模式 + 指令模式）
- ✅ 时间智能解析
- ✅ 人格化消息生成
- ✅ 循环日程
- ✅ 戳一戳提醒
- ✅ TTS语音播报

### 2. 昵称配置恢复

**配置项：** `user_alias`

**格式：**
```json
{
  "user_alias": [
    "1498976088,小明",
    "123456789,老板",
    "987654321,小王"
  ]
}
```

**配置方式：**
1. AstrBot管理面板 → 插件配置 → user_alias
2. 按格式添加：`QQ号,昵称`

**使用说明：**
- 此昵称仅用于提醒消息中显示
- 不会影响LLM理解或其他插件
- 如果未配置，使用群昵称

### 3. 提醒消息格式优化

**新格式：**
```
[戳一戳]
@用户 昵称 时间到了，该去xxx啦~
```

**消息组成：**
1. **戳一戳**（可选，enable_poke配置）
2. **@组件** - 确保用户收到通知
3. **昵称** - 从user_alias配置读取
4. **LLM文本** - 人格化的提醒内容

**示例：**
```
配置：
user_alias: ["1498976088,小明"]

消息：
@小明 小明 15:30到了，该去上厕所啦~
       ↑    ↑
      @组件 配置的昵称
```

### 4. LLM监控简化

**之前（v3.2.7）：**
```
步骤1: 判断是否为日程请求
步骤2: 提取日程信息（含target/昵称）
步骤3: 检查@对象 + 昵称反向查找
步骤4: 创建日程
步骤5: 生成确认回复
```

**现在（v3.3.0）：**
```
步骤1: 判断是否为日程请求
步骤2: 提取日程信息（target固定为"用户"）
步骤3: 创建日程（仅支持提醒自己）
步骤4: 生成确认回复
```

**优势：**
- 减少LLM调用步骤
- 降低超时风险
- 逻辑更清晰可靠

## 📋 配置说明

### 完整配置示例

```json
{
  "llm_provider": null,
  "schedule_detection_llm": null,
  "tts_provider": null,
  "enable_llm_monitor": true,
  "enable_tts": false,
  "enable_web_search": false,
  "enable_personality": true,
  "enable_poke": true,
  "max_reminders": 20,
  "trigger_keywords": [
    "提醒我", "提醒", "帮我提醒", 
    "设置提醒", "定时提醒", "callme"
  ],
  "user_alias": [
    "1498976088,小明",
    "123456789,老板"
  ],
  "data_persistence": true
}
```

### 关键配置项

| 配置项 | 类型 | 说明 | 默认值 |
|--------|------|------|--------|
| `user_alias` | list | 昵称映射列表 | `[]` |
| `enable_poke` | bool | 是否启用戳一戳 | `true` |
| `enable_llm_monitor` | bool | 是否启用LLM监控 | `true` |
| `enable_personality` | bool | 是否启用人格化回复 | `true` |

## 📝 使用示例

### 基础用法

**LLM监控模式：**
```
用户: axx一分钟后提醒我上厕所
Bot: 好的，我会在15:31提醒你上厕所~

（1分钟后）
Bot: [戳一戳]
     @用户 小明 15:31到了，该去上厕所啦~
```

**指令模式：**
```
用户: /callme 10分钟后 喝水
Bot: ✅ 一次性提醒已创建
     📌 喝水
     ⏰ 2月2日 15:40
     🏷️ R0001
```

### 昵称配置

**步骤1：配置昵称**
```json
{
  "user_alias": [
    "1498976088,小明"
  ]
}
```

**步骤2：创建提醒**
```
用户: axx5分钟后提醒我开会
```

**步骤3：收到提醒**
```
@1498976088 小明 15:35到了，该去开会啦~
            ↑
         配置的昵称
```

### 不配置昵称的情况

**配置：**
```json
{
  "user_alias": []
}
```

**提醒消息：**
```
@1498976088 群昵称 15:35到了，该去开会啦~
            ↑
         使用群昵称
```

## 🔍 故障排除

### 问题1：昵称未显示

**症状：** 提醒消息中显示的是群昵称，而不是配置的昵称

**排查：**
1. 检查配置格式是否正确
2. 确认QQ号完全匹配（无空格）
3. 查看日志：`[用户昵称] 用户xxx使用配置昵称: 'xxx'`

**解决：**
```json
// 错误格式
"user_alias": ["1498976088, 小明"]  // 逗号后有空格

// 正确格式  
"user_alias": ["1498976088,小明"]   // 无空格
```

### 问题2：消息重复

**症状：** 同一条提醒消息被处理多次

**原因：** 消息去重未生效

**查看日志：**
```
[LLM监控模式] ⚠️ 检测到重复消息（0.1秒前已处理），跳过
```

**解决：** 
- 如果看到去重日志，说明正常
- 如果没有去重日志，可能有多个插件实例

### 问题3：LLM监控不触发

**症状：** 发送"提醒我xxx"没有反应

**排查：**
1. 检查`enable_llm_monitor`是否为true
2. 查看日志是否有关键词匹配
3. 确认LLM Provider配置正确

**日志示例：**
```
✅ 正常：
[LLM监控模式] 关键词预匹配成功: 一分钟后提醒我上厕所

❌ 未触发：
（无相关日志）
```

## ⚠️ 破坏性变更

### 从v3.2.x升级需要注意

1. **移除了uni_nickname依赖**
   - 不再需要安装uni_nickname插件
   - 现有的uni_nickname配置不会被使用

2. **不再支持"提醒别人"**
   - `axx提醒小龙xxx` - ❌ 不支持
   - `axx提醒@某人xxx` - ❌ 不支持
   - `axx提醒我xxx` - ✅ 支持

3. **昵称配置变更**
   - 需要在本插件配置中设置`user_alias`
   - 格式：`"QQ号,昵称"`

### 迁移步骤

**如果你从v3.2.7升级：**

1. **备份uni_nickname配置**（如果有）
   ```bash
   cat data/config/uni_nickname.json
   ```

2. **迁移昵称到本插件**
   ```json
   // uni_nickname配置
   {
     "nickname_mappings": ["1498976088,小明"]
   }
   
   // 迁移到reminder插件
   {
     "user_alias": ["1498976088,小明"]
   }
   ```

3. **更新插件到v3.3.0**

4. **测试昵称显示**
   ```
   发送: axx1分钟后提醒我测试
   检查: 提醒消息中是否显示配置的昵称
   ```

## 📊 性能优化

### LLM调用减少

**v3.2.7：**
- 判断是否为日程：1次
- 提取信息：1次
- 获取@对象：可能调用群API
- 昵称反查：读取配置
- 生成确认：1次
- **总计：3次LLM + API调用**

**v3.3.0：**
- 判断是否为日程：1次
- 提取信息：1次
- 生成确认：1次
- **总计：3次LLM**（无额外API）

### 超时问题改善

**移除的超时风险点：**
- ❌ 获取@对象（10秒超时）
- ❌ 读取uni_nickname配置（可能IO阻塞）
- ❌ 昵称反向查找（遍历映射）

**结果：**
- 创建日程更快
- 超时概率降低
- 用户体验提升

## 🎉 总结

v3.3.0是一个**回归本质、专注核心**的版本：

**做减法：**
- 移除复杂的外部依赖
- 移除不稳定的功能
- 简化LLM监控逻辑

**做加法：**
- 更清晰的消息格式
- 更详细的调试日志
- 更完善的文档

**理念：**
> "提醒自己"功能做到极致，比"提醒别人"做得半吊子要好。

**未来计划：**
- 在v3.3.x稳定后，考虑重新设计"提醒别人"功能
- 可能采用更简单的实现方式（如配置群成员昵称列表）
- 先把基础功能做好，再添加高级功能

## 📖 相关文档

- [README.md](README.md) - 插件完整文档
- [CHANGELOG.md](CHANGELOG.md) - 完整更新日志
- [_conf_schema.json](_conf_schema.json) - 配置项定义

## 🙏 感谢

感谢所有测试和反馈的用户！你们的意见帮助我们找到了最合适的方向。
