# v3.2.4 测试指南

## 快速测试清单

### ✅ 1. 戳一戳功能测试（新修复）

#### 测试场景1：提醒自己
```
发送消息：提醒我 1分钟后 测试戳一戳
等待1分钟
预期结果：
  ✓ 收到戳一戳（实际被戳，不是只有消息）
  ✓ 收到提醒消息
  ✓ 如果enable_tts=true，还会收到语音
```

#### 测试场景2：提醒别人（群聊）
```
发送消息：提醒 @张三 1分钟后 开会
等待1分钟
预期结果：
  ✓ 张三收到戳一戳
  ✓ 张三收到提醒消息（提到是谁让提醒的）
```

#### 检查日志
```
搜索关键词：发送戳一戳
应该看到：
  ✓ [INFO] 发送群戳一戳: group=xxx, user=xxx
  ✓ [INFO] 戳一戳发送成功: target=xxx
```

---

### ✅ 2. LLM监控超时测试（新修复）

#### 测试场景：大群环境@别人
```
前提条件：在500+人的大群中
发送消息：提醒 @群成员 明天下午2点开会
预期结果：
  ✓ 不会超时（10秒内返回）
  ✓ 收到确认消息
  ✓ 不会卡死AstrBot控制界面
```

#### 检查日志
```
搜索关键词：[LLM监控模式] 步骤3
应该看到：
  ✓ [INFO] 步骤3: 检查@对象...
  ✓ [INFO] 找到@对象: xxx (xxxx) 或
  ✓ [WARNING] 获取@对象超时，默认为自己
  
关键：不应该出现30秒无响应的情况
```

---

### ✅ 3. 昵称映射测试（已有功能验证）

#### 配置设置
在 `_conf_schema.json` 或AstrBot配置界面中：
```json
{
  "user_alias": [
    "你的QQ号,老板",
    "朋友的QQ号,小助手"
  ]
}
```

#### 测试场景1：提醒自己
```
重启插件（使配置生效）
发送消息：提醒我 1分钟后 测试昵称
等待1分钟
预期结果：
  ✓ 提醒消息中包含"老板"而不是你的QQ昵称
```

#### 测试场景2：提醒别人
```
发送消息：提醒 @小助手 1分钟后 测试
等待1分钟
预期结果：
  ✓ 提醒消息中提到"是老板拜托我提醒小助手..."
  ✓ 使用的是配置中的昵称而不是QQ昵称
```

---

## 对比测试（v3.2.3 vs v3.2.4）

### v3.2.3 的问题
1. ❌ 戳一戳不工作（API调用被注释）
2. ❌ @别人建立日程时LLM监控超时
3. ❌ 大群环境获取成员信息慢导致卡死

### v3.2.4 的修复
1. ✅ 戳一戳真正调用API（`client.group_poke()` / `client.friend_poke()`）
2. ✅ 获取@对象有10秒超时保护，超时时默认为自己
3. ✅ 不再阻塞整个流程

---

## 日志检查要点

### 正常情况的日志模式
```
[INFO] [LLM监控模式] 步骤1: 开始判断是否为日程请求...
[INFO] [LLM监控模式] 检测到日程设定请求: ...
[INFO] [LLM监控模式] 步骤2: 开始提取日程信息...
[INFO] [LLM监控模式] 提取结果: {...}
[INFO] [LLM监控模式] 步骤3: 检查@对象...
[INFO] [LLM监控模式] 找到@对象: xxx (xxxx) 或 未找到@对象，默认为自己
[INFO] [LLM监控模式] 步骤4: 开始创建日程...
[INFO] [LLM监控模式] 创建结果: success=True
[INFO] [LLM监控模式] 步骤5: 生成确认回复...
[INFO] [LLM监控模式] 处理完成，已发送确认消息
```

### 超时情况的日志模式（现在不应该出现）
```
[WARNING] [LLM监控模式] 获取@对象超时，默认为自己
（继续后续步骤，不会卡死）
```

### 戳一戳日志模式
```
[INFO] 发送戳一戳: target=12345678
[INFO] 发送群戳一戳: group=123456, user=12345678
[INFO] 戳一戳发送成功: target=12345678
```

---

## 常见问题排查

### Q1: 戳一戳不工作
**检查项：**
1. 配置中 `enable_poke` 是否为 `true`
2. 是否为aiocqhttp平台（其他平台不支持）
3. 日志中是否有"发送戳一戳"相关记录
4. 是否有错误日志（如"无法获取aiocqhttp客户端实例"）

### Q2: 昵称映射不生效
**检查项：**
1. 配置格式是否正确：`["用户ID,称呼"]`（注意逗号和引号）
2. 用户ID是否正确（必须完全匹配）
3. 是否重启插件使配置生效
4. 日志中搜索 `_get_user_display_name`，查看是否找到映射

### Q3: @别人仍然超时
**检查项：**
1. 是否更新到v3.2.4（检查 `metadata.yaml` 中的version）
2. 网络是否正常（napcat API响应慢）
3. 日志中是否有"获取@对象超时"警告（有的话说明超时保护生效）
4. 是否有其他异常日志

---

## 性能测试建议

### 压力测试1：大群环境
- 在1000+人的大群中
- 连续发送5条 `提醒 @xxx` 消息
- 预期：每条都在10秒内返回，不卡死

### 压力测试2：高频提醒
- 创建10条1分钟后的提醒
- 预期：所有提醒都准时触发，都有戳一戳

### 压力测试3：长时间运行
- 运行24小时
- 预期：所有定时提醒准时触发，无内存泄漏

---

## 升级后验证步骤

1. ✅ 检查版本号（`metadata.yaml` 应为 `v3.2.4`）
2. ✅ 重启AstrBot
3. ✅ 测试戳一戳（提醒自己）
4. ✅ 测试戳一戳（提醒别人）
5. ✅ 测试@别人建立日程（不超时）
6. ✅ 测试昵称映射（如果配置了）
7. ✅ 查看日志确认无ERROR

---

## 回滚方案

如果v3.2.4有问题，回滚到v3.2.3：
```bash
cd /path/to/astrbot/data/plugins/astrbot_plugin_reminder
git checkout v3.2.3
# 重启AstrBot
```

或手动恢复备份：
```bash
cp main.py.v3.2.3.bak main.py
# 修改 metadata.yaml version: v3.2.3
# 重启AstrBot
```

---

## 反馈模板

如果发现问题，请提供以下信息：
```
**环境信息**
- AstrBot版本：
- napcat版本：
- 平台：aiocqhttp / 其他
- Python版本：

**问题描述**
- 具体操作：
- 预期结果：
- 实际结果：

**日志片段**
（请提供相关的ERROR/WARNING日志）

**配置信息**
（请提供脱敏后的配置）
```

---

## 参考文档
- [v3.2.4_更新说明.md](v3.2.4_更新说明.md) - 详细的修复说明
- [CHANGELOG.md](CHANGELOG.md) - 完整的版本更新历史
- [pokepro插件](https://github.com/Zhalslar/astrbot_plugin_pokepro) - 戳一戳功能参考
