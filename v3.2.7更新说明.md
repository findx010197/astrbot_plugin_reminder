# v3.2.7 更新说明 - uni_nickname插件集成

## 🎯 核心改进

### ✅ 解决的问题

1. **昵称重复问题**
   - 之前：提醒插件和uni_nickname插件都维护昵称映射，导致提醒消息中昵称显示两次
   - 现在：移除提醒插件内置的`user_alias`配置，统一使用uni_nickname插件
   - 效果：消息干净，昵称只显示一次

2. **@成员提醒失效问题**  
   - 之前：使用"提醒 @小龙 xxxx"格式，LLM监控无法触发（30秒超时）
   - 原因：@组件干扰关键词匹配和LLM理解
   - 现在：支持"提醒 小龙 xxxx"格式（基于昵称），LLM可以直接识别
   - 效果：LLM监控能正常工作，@成员提醒不再超时

## 🔧 主要变更

### 1. 移除内置昵称映射

**删除的配置项：**
```json
"user_alias": {
  "description": "用户ID与称呼映射表",
  "type": "list",
  "hint": "每一项格式为：用户ID,称呼。例如：123456789,老板",
  "default": []
}
```

**注意：** 如果您之前使用了`user_alias`配置，请迁移到uni_nickname插件

### 2. 集成uni_nickname插件

**新增方法：**
- `_get_uni_nickname_mappings()`: 从uni_nickname插件读取昵称映射
- `_get_userid_by_nickname()`: 根据昵称反向查找用户ID

**工作原理：**
```python
# uni_nickname配置格式
nickname_mappings: ["3342496519,小龙", "123456789,老板"]

# 提醒插件读取映射
mappings = {"3342496519": "小龙", "123456789": "老板"}

# 用户输入: "提醒 小龙 明天开会"
# LLM提取: {"target": "小龙", "time": "明天", "event": "开会"}
# 反向查找: "小龙" → "3342496519"
# 创建提醒: 为用户3342496519创建提醒
```

### 3. 增强LLM提取能力

**更新的提示词：**
```text
3. target: 提醒的目标对象
   - 如果消息中提到具体人名/昵称（如"提醒小龙"、"提醒老板"），提取该人名/昵称
   - 如果消息中有@提及，提取被@的人的显示名称
   - 否则填"用户"表示提醒自己

已知昵称列表：小龙, 老板, 小明...（自动从uni_nickname读取）
```

**处理流程：**
```
1. 检查是否有@组件 → 如果有，使用@对象
2. 如果没有@，检查LLM提取的target是否为昵称
3. 如果是昵称，通过_get_userid_by_nickname()查找用户ID
4. 如果找到，使用该用户ID；否则提醒自己
```

### 4. 优化提醒消息格式

**之前：**
```
@小龙 小龙 明天开会的时间到了
      ↑↑ 昵称重复
```

**现在：**
```
@小龙 明天开会的时间到了
     ↑ 昵称由uni_nickname在LLM层面处理
```

## 📖 使用方式

### 方式1：@成员（兼容旧方式）
```
提醒 @小龙 明天开会
axx提醒 @用户名 10分钟后 喝水
```

### 方式2：昵称（推荐新方式）✨
```
提醒 小龙 明天开会
axx提醒 老板 10分钟后 提交报告
```

### 方式3：指令模式
```
/callme 10分钟后 @小龙 开会
```

## ⚙️ 配置uni_nickname

### 1. 安装uni_nickname插件
```bash
# 从GitHub安装
https://github.com/Hakuin123/astrbot_plugin_uni_nickname
```

### 2. 配置昵称映射
```bash
# 管理员设置（需要管理员权限）
/nickname set 3342496519 小龙
/nickname set 123456789 老板

# 用户自己设置
/nickname setme 小明

# 查看所有映射
/nickname list
```

### 3. 配置格式（直接编辑配置文件）
```json
{
  "nickname_mappings": [
    "3342496519,小龙",
    "123456789,老板",
    "987654321,小明"
  ],
  "working_mode": "prompt"
}
```

## 🔍 工作模式说明

uni_nickname插件有两种工作模式：

1. **prompt模式（推荐）**
   - 在system_prompt中告诉LLM昵称映射
   - 不修改原消息文本
   - 适合大部分场景

2. **global模式**
   - 直接替换消息中的QQ号为昵称
   - 会修改原消息文本
   - 适合需要完全替换的场景

## 🐛 故障排除

### 问题1：昵称提醒不生效

**检查清单：**
- [ ] uni_nickname插件是否已安装并启用
- [ ] 昵称映射是否正确配置（`/nickname list`查看）
- [ ] 提醒插件是否已更新到v3.2.7
- [ ] 消息中昵称拼写是否与配置完全一致

**调试日志：**
```
[昵称映射] 成功加载 3 个昵称映射
[LLM监控模式] 尝试根据昵称查找用户: 小龙
[LLM监控模式] 找到昵称对应的用户: 小龙 → 3342496519
```

### 问题2：找不到uni_nickname配置

**可能原因：**
1. uni_nickname插件未安装
2. 配置文件路径不正确
3. 配置文件格式错误

**解决方法：**
```python
# 检查配置文件是否存在
路径: data/config/uni_nickname.json

# 检查配置格式
{
  "nickname_mappings": [
    "用户ID,昵称",
    "123456789,小明"
  ]
}
```

### 问题3：@提醒还是超时

**可能原因：**
- LLM服务响应慢
- 网络问题
- 消息太复杂

**解决方法：**
- 使用昵称方式：`提醒 小龙 xxx` 而不是 `提醒 @小龙 xxx`
- 或使用指令模式：`/callme 10分钟后 @小龙 开会`

## 📊 性能优化

### 缓存机制
```python
# uni_nickname插件有缓存
_mappings_cache = {"123456789": "小龙"}

# 提醒插件每次调用_get_uni_nickname_mappings()时都会重新读取
# 如果昵称映射频繁变动，性能影响很小
# 如果担心性能，可以在ReminderPlugin中添加缓存
```

### 建议配置
```json
{
  "max_reminders": 20,  // 单用户最大提醒数
  "enable_llm_monitor": true,  // 启用LLM监控
  "trigger_keywords": [
    "提醒", "提醒我", "帮我提醒", "喊我", "叫我"
  ]
}
```

## 🔄 迁移指南

### 从v3.2.6迁移

1. **备份现有昵称配置**
```json
// 旧配置 (user_alias)
"user_alias": [
  "3342496519,小龙",
  "123456789,老板"
]
```

2. **安装uni_nickname插件**

3. **迁移昵称映射**
```bash
# 方法1：命令行设置
/nickname set 3342496519 小龙
/nickname set 123456789 老板

# 方法2：直接编辑uni_nickname配置文件
# 路径: data/config/uni_nickname.json
{
  "nickname_mappings": [
    "3342496519,小龙",
    "123456789,老板"
  ]
}
```

4. **更新提醒插件到v3.2.7**

5. **验证功能**
```
# 测试昵称提醒
提醒 小龙 1分钟后 测试

# 检查日志
[昵称映射] 成功加载 2 个昵称映射
[LLM监控模式] 找到昵称对应的用户: 小龙 → 3342496519
```

## ⚠️ 破坏性变更

1. **移除user_alias配置**
   - 旧配置将被忽略
   - 必须迁移到uni_nickname插件

2. **提醒消息格式变更**
   - 不再添加固定昵称文本
   - 昵称由uni_nickname在LLM层面处理

## 📝 更新日志

**v3.2.7 (2026-02-02)**
- ✨ 集成uni_nickname插件，支持基于昵称的提醒
- 🐛 修复昵称重复显示问题
- 🐛 修复@成员提醒LLM监控超时问题
- 🗑️ 移除内置user_alias配置
- 📚 增强LLM提取能力，支持昵称识别
- 🔧 优化提醒消息格式

**相关Issue：**
- 解决uni_nickname与提醒插件昵称重复问题
- 解决@成员提醒LLM监控失效问题

## 🙏 致谢

感谢 [@Hakuin123](https://github.com/Hakuin123) 开发的 [uni_nickname插件](https://github.com/Hakuin123/astrbot_plugin_uni_nickname)
