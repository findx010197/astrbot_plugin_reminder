# v3.2.3 深度分析与修复方案

## 问题复现与定位

### 现象描述
- **提醒自己**：正常工作，无卡死
- **@成员提醒别人**：出现30秒卡死，AstrBot控制界面冻结

### 根本原因分析

经过深入代码审查和对比AtTool插件实现，发现**三个关键问题**：

#### 🔴 核心问题1：@组件构造错误

**当前实现（main.py:1150行）：**
```python
if schedule.target_id:
    chain.append(Comp.At(qq=schedule.target_id))  # ❌ 错误：At组件不接受qq参数
```

**正确实现（参考AtTool插件）：**
```python
# At组件的正确构造方式：
from astrbot.core.message.components import At
At(qq=user_id)  # ✅ qq参数名正确
```

**但关键发现**：我们的导入方式有问题！

**当前导入：**
```python
import astrbot.api.message_components as Comp  # 这个是API层的抽象
```

**AtTool的导入：**
```python
from astrbot.core.message.components import At  # 这个是Core层的实现
```

**结论**：`astrbot.api.message_components.At` 与 `astrbot.core.message.components.At` 是不同的类！
- API层的At可能不支持qq参数或有不同的初始化方式
- Core层的At才是真正的实现类

#### 🔴 核心问题2：_get_at_target实现不完整

**当前实现（main.py:344-375行）：**
```python
def _get_at_target(self, event: AstrMessageEvent) -> tuple[Optional[str], Optional[str]]:
    for component in event.message_obj.message:
        if isinstance(component, Comp.At):  # ❌ 检查的是API层的At
            target_id = str(getattr(component, "qq", getattr(component, "id", "")))
            if target_id:
                return target_id, "TA"  # ❌ 名称固定为"TA"，没有获取真实昵称
    return None, None
```

**问题：**
1. 检查的是错误的At类型
2. 即使找到At，也没有获取被@用户的真实昵称
3. 缺少群成员信息查询（QQ协议可以查到真实昵称）

**AtTool的正确实现：**
```python
# 1. 通过aiocqhttp API获取群成员列表
raw_members = await event.bot.api.call_action('get_group_member_list', group_id=group_id)

# 2. 从中查找指定user_id的昵称
for m in raw_members:
    if str(m.get("user_id")) == target_id:
        nickname = m.get("nickname", "")
        card = m.get("card", "")  # 群名片
        return target_id, card if card else nickname
```

#### 🔴 核心问题3：戳一戳功能完全缺失

**用户需求流程：**
1. 戳一戳提醒对象（一条消息）
2. @提醒对象+昵称+提醒文本（一条消息）
3. 语音（如果开启TTS）

**当前实现：**
```python
async def _send_reminder(self, schedule: ScheduleItem):
    chain = []
    if schedule.target_id:
        chain.append(Comp.At(qq=schedule.target_id))  # ❌ 只有@，没有戳一戳
        chain.append(Comp.Plain(" "))
    chain.append(Comp.Plain(message))
    # ... 发送
```

**缺失的戳一戳实现（napcat API）：**
```python
# 群戳一戳
await event.bot.api.call_action('group_poke', group_id=group_id, user_id=target_id)

# 私聊戳一戳
await event.bot.api.call_action('friend_poke', user_id=target_id)
```

### 为什么@自己不卡，@别人卡？

**深层原因：**

1. **@自己时**：
   - `target_id = sender_id`（都是自己）
   - 即使At组件构造错误，平台可能容错处理
   - 消息能正常发送，不会超时

2. **@别人时**：
   - `target_id != sender_id`
   - At组件构造错误导致消息格式异常
   - aiocqhttp/napcat尝试发送格式错误的消息
   - 可能触发平台的消息验证失败
   - 进入重试或等待超时（30秒）
   - 因为v3.2.2之前缺少异常处理，导致event.stop_event()未调用
   - 事件传播未停止，AstrBot等待其他插件处理
   - **最终表现为整个控制界面卡死30秒**

## 修复方案

### 方案1：导入层修复（推荐）

```python
# 改用Core层的消息组件
from astrbot.core.message.components import Plain, At, Record
# 不再使用 import astrbot.api.message_components as Comp
```

### 方案2：完整实现_get_at_target

```python
async def _get_at_target(self, event: AstrMessageEvent) -> tuple[Optional[str], Optional[str]]:
    """从消息中获取第一个被@的用户ID和真实昵称"""
    from astrbot.core.message.components import At
    from astrbot.core.platform.sources.aiocqhttp.aiocqhttp_message_event import AiocqhttpMessageEvent
    
    # 1. 从消息链中提取At组件
    target_id = None
    for component in event.message_obj.message:
        if isinstance(component, At):
            target_id = str(getattr(component, "qq", getattr(component, "id", "")))
            if target_id:
                break
    
    if not target_id:
        return None, None
    
    # 2. 获取真实昵称（仅aiocqhttp平台支持）
    target_name = "TA"  # 默认名称
    
    if isinstance(event, AiocqhttpMessageEvent):
        group_id = event.get_group_id()
        if group_id:  # 群聊环境
            try:
                members = await event.bot.api.call_action('get_group_member_list', group_id=group_id)
                for m in members:
                    if str(m.get("user_id")) == target_id:
                        card = m.get("card", "")
                        nickname = m.get("nickname", "")
                        target_name = card if card else nickname
                        break
            except Exception as e:
                logger.warning(f"获取群成员昵称失败: {e}")
        else:  # 私聊环境
            try:
                stranger_info = await event.bot.api.call_action('get_stranger_info', user_id=int(target_id))
                target_name = stranger_info.get("nickname", "TA")
            except Exception as e:
                logger.warning(f"获取私聊昵称失败: {e}")
    
    return target_id, target_name
```

### 方案3：实现戳一戳功能

```python
async def _send_poke(self, event: AstrMessageEvent, target_id: str):
    """发送戳一戳"""
    from astrbot.core.platform.sources.aiocqhttp.aiocqhttp_message_event import AiocqhttpMessageEvent
    
    if not isinstance(event, AiocqhttpMessageEvent):
        logger.warning("当前平台不支持戳一戳")
        return
    
    try:
        group_id = event.get_group_id()
        if group_id:
            # 群戳一戳
            await event.bot.api.call_action('group_poke', group_id=group_id, user_id=int(target_id))
            logger.info(f"已发送群戳一戳: group={group_id}, user={target_id}")
        else:
            # 私聊戳一戳
            await event.bot.api.call_action('friend_poke', user_id=int(target_id))
            logger.info(f"已发送私聊戳一戳: user={target_id}")
    except Exception as e:
        logger.error(f"发送戳一戳失败: {e}")

async def _send_reminder(self, schedule: ScheduleItem):
    """发送提醒消息（包含戳一戳+@+文本）"""
    from astrbot.core.message.components import Plain, At, Record
    
    try:
        umo = schedule.unified_msg_origin
        message = await self._generate_reminder_message(schedule)
        
        # ===== 1. 戳一戳 =====
        if schedule.target_id and self.config.get("enable_poke", True):
            # 需要从umo重建event来调用戳一戳
            # 或者直接存储platform信息
            # 这里需要重构存储逻辑，暂时跳过
            pass
        
        # ===== 2. @提醒对象+文本消息 =====
        chain = []
        if schedule.target_id:
            chain.append(At(qq=int(schedule.target_id)))  # 注意：qq参数要int类型
            chain.append(Plain(" "))
        
        # 添加提醒文本
        chain.append(Plain(message))
        
        message_chain = MessageChain()
        message_chain.chain = chain
        await self.context.send_message(umo, message_chain)
        
        logger.info(f"已发送提醒消息: {message[:50]}...")
        
        # ===== 3. TTS语音 =====
        if self.config.get("enable_tts", False):
            await self._send_tts_reminder(schedule, message)
    
    except Exception as e:
        logger.error(f"发送提醒消息失败: {e}", exc_info=True)
```

## LLM生成提醒文本流程优化

**当前流程问题：**
- 在_generate_reminder_message中调用LLM
- 每次提醒触发时都要等待LLM响应
- 如果LLM慢，用户等待时间长

**优化建议：**
1. 增加兜底模板，LLM超时时使用
2. 缓存常见提醒文本
3. 对于循环提醒，首次生成后缓存

## 下一步实施计划

1. ✅ 修复导入层（Core层替代API层）
2. ✅ 重写_get_at_target（支持真实昵称）
3. ✅ 实现_send_poke（戳一戳功能）
4. ✅ 优化_send_reminder（完整流程）
5. ✅ 增加配置项（enable_poke等）
6. ✅ 测试验证

## 参考资料

1. AtTool插件实现：https://github.com/nuomicici/astrbot_plugin_AtTool
2. Napcat API文档：https://napcat.apifox.cn/
3. AstrBot插件开发文档：core.message.components 使用
